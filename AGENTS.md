# CheapAI 工程规则

## 删除逻辑规范

1. 后台所有“删除”操作必须按“删除即不可见”处理。
   - 可以采用逻辑删除，例如设置 `DeletedAt`、`deleted_at` 或等价删除标记。
   - 页面列表、详情入口、筛选项和用户端展示都不能再显示已删除数据。
   - 不允许把用户点击的“删除”仅实现为页面仍可见的“归档”或普通状态切换。

2. “归档”和“删除”语义必须区分。
   - 归档：业务仍需要在后台可检索、可审计时使用，按钮和文案必须明确写“归档”。
   - 删除：操作后默认列表不再出现，按钮和文案必须明确写“删除”。
   - 如果底层没有物理删除能力，删除接口也必须使用逻辑删除字段或等价条件，确保查询层排除。

3. 查询层必须统一过滤已删除数据。
   - 后台管理列表默认排除已删除记录。
   - 后台详情接口对已删除记录应返回不存在或不可访问。
   - 用户端公开接口必须排除已删除记录。
   - 批量操作完成后必须刷新列表，并清空已选项。

4. 中转站模型报价删除规则。
   - 在编辑中转站时删除模型报价，保存后该报价不能再回显。
   - 可以将未触达报价标记为 `archived` 或逻辑删除，但读取详情时必须过滤。
   - 状态筛选的“全部”不能包含已删除数据；如需查看归档数据，必须使用明确的归档入口。

5. 批量操作规范。
   - 后台可操作列表应支持多选。
   - 批量删除必须有确认提示，提示中包含选中数量。
   - 批量删除应复用单条删除语义，不允许批量路径与单条路径行为不一致。

## 实施要求

- 新增删除能力时优先补齐后端删除接口，再接入前端按钮。
- 不要只在前端 `filter` 掉数据来伪装删除；后端查询也必须过滤。
- 删除相关改动需要验证：
  - 删除后当前列表消失。
  - 刷新页面后仍不显示。
  - 详情接口不能继续返回已删除记录。
  - 用户端公开页面不显示已删除记录。
